---
quick_id: 260618-g8a
status: planned
date: 2026-06-18
---

# Quick Task 260618-g8a: Deploy order list item UI refactor

## Goal

Deploy the committed order list item UI refactor through the repository's static `docs/` deployment flow.

## Tasks

1. Run `yarn build`.
2. Copy `build/` into `docs/`, excluding `build/manifest.json`.
3. Stage `./src/*` and `./docs/*`, commit generated deployment output if changed, and push.

## Verification

- Production build completes successfully.
- Deployment push completes successfully.

