---
phase: 02-order-state-and-trello-sync-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Common/Helpers/OrderDomainHelper.ts
  - src/Common/Helpers/OrderDomainHelper.test.ts
  - src/Common/Helpers/OrderHelper.ts
  - src/Hooks/useOrder.ts
  - src/Hooks/useOrder.test.ts
autonomous: true
requirements:
  - ORD-01
  - ORD-02
user_setup: []
must_haves:
  truths:
    - "D-04: Existing order creation, shipping-code update, shipped/returned/refund/COD, and attachment behavior remains available after pure helper extraction."
    - "D-17: Pure transition and calculation functions exist outside src/Hooks/useOrder.ts and can be tested without rendering React components."
    - "D-22: Focused tests cover status changes, customer VIP/blacklist/buy totals, priority/position calculation inputs, shipping-code behavior, COD paid, refund, labels, and card description where extracted."
    - "D-25: This plan does not add broad UI workflow tests; it only preserves hook behavior and extracts testable order-domain logic."
  artifacts:
    - path: src/Common/Helpers/OrderDomainHelper.ts
      provides: "Pure order transition, amount, card-description, and label-intent helpers"
    - path: src/Common/Helpers/OrderDomainHelper.test.ts
      provides: "Unit coverage for order-domain behavior without React rendering"
    - path: src/Hooks/useOrder.ts
      provides: "Existing order hook behavior routed through extracted helpers where safe"
  key_links:
    - from: src/Hooks/useOrder.ts
      to: src/Common/Helpers/OrderDomainHelper.ts
      via: "useOrder delegates pure calculations and transition output before Redux dispatch or Trello calls"
    - from: src/Common/Helpers/OrderDomainHelper.ts
      to: src/Common/Constants/AppConstants.ts
      via: "ORDER_STATUS, ORDER_RETURN_REASON, ORDER_PAYMENT_METHOD, ORDER_PRIORITY_STATUS remain the source of business constants"
---

## Phase Goal

**As a** cassette-store operator, **I want to** keep using current order actions while order rules become testable, **so that** later Trello sync reliability changes do not break business behavior.

<objective>
Extract pure order-domain calculations and transition functions from `src/Hooks/useOrder.ts` while preserving runtime behavior.

Purpose: Phase 2 needs a safe seam before Trello orchestration changes. This plan satisfies `ORD-01`, `ORD-02`, D-17, and D-22 by moving pure order logic into tested helpers without changing the operator-facing workflow.

Output: `OrderDomainHelper` with focused tests and a narrower `useOrder` that delegates pure decisions before dispatching Redux updates or calling Trello.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-order-state-and-trello-sync-reliability/02-CONTEXT.md
@.planning/phases/02-order-state-and-trello-sync-reliability/02-RESEARCH.md
@.planning/phases/02-order-state-and-trello-sync-reliability/02-VALIDATION.md
@.planning/phases/02-order-state-and-trello-sync-reliability/02-PATTERNS.md
@src/Hooks/useOrder.ts
@src/Hooks/useOrder.test.ts
@src/Common/Helpers/OrderHelper.ts
@src/Common/Constants/AppConstants.ts
@src/Store/Models/Order.ts
@src/Store/Models/Customer.ts
@src/Store/Models/OrderItem.ts
@src/Store/Reducers/OrderReducer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pure order-domain helper and transition tests</name>
  <files>src/Common/Helpers/OrderDomainHelper.ts, src/Common/Helpers/OrderDomainHelper.test.ts, src/Common/Helpers/OrderHelper.ts</files>
  <read_first>src/Hooks/useOrder.ts, src/Common/Helpers/OrderHelper.ts, src/Common/Constants/AppConstants.ts, src/Store/Models/Order.ts, src/Store/Models/Customer.ts, src/Store/Models/OrderItem.ts, src/Store/Reducers/OrderReducer.test.ts, .planning/phases/02-order-state-and-trello-sync-reliability/02-PATTERNS.md</read_first>
  <action>Create `src/Common/Helpers/OrderDomainHelper.ts` as a pure helper module for order-domain behavior currently embedded in `useOrder`. Include functions for `buildOrderTrelloDescription`, `calculateOrderPaymentAmountFromCustomer`, `getAutoCODAmount`, `getOrderTrelloLabelKeys`, `isVipOrder`, `isPriority`, `isUrgent`, `isCustomerReturnLessThan4`, `isBankTransferInAdvance`, `canMarkAsWaitingForReturn`, `canMarkAsReturned`, `canMarkAsShipped`, `canMarkAsPayCOD`, `markOrderAsShippedTransition`, `markOrderAsRefuseToReceiveTransition`, `markOrderAsBrokenItemsTransition`, `markOrderAsWaitingForReturnTransition`, `markOrderAsReturnedTransition`, `markOrderAsPayCODTransition`, `changeShippingCodeTransition`, and `refundTransition`. Keep functions side-effect free: no Redux, no `store`, no React, no Trello calls, and no mutation of input objects. Add `src/Common/Helpers/OrderDomainHelper.test.ts` with local `Order` and `Customer` builders covering D-17 and D-22 behaviors. Keep `OrderHelper` intact for existing item/priority/shipping helpers; import or delegate to it when useful rather than duplicating shipping amount logic.</action>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runInBand src/Common/Helpers/OrderDomainHelper.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/Common/Helpers/OrderDomainHelper.ts` exports the functions named in this task action.</criterion>
    <criterion>`OrderDomainHelper.test.ts` asserts that shipped transitions set `ORDER_STATUS.SHIPPED`, clear `returnReason`, increment `customer.buyCount`, add `order.paymentAmount` to `customer.buyAmount`, and set VIP when the current `useOrder` threshold is met.</criterion>
    <criterion>`OrderDomainHelper.test.ts` asserts that refuse-to-receive transitions set `WAITING_FOR_RETURNED`, set `ORDER_RETURN_REASON.REFUSE_TO_RECEIVE`, set `customer.isInBlacklist` true, and set `customer.isVIP` false.</criterion>
    <criterion>`OrderDomainHelper.test.ts` asserts that shipping-code transition sets `ORDER_STATUS.CREATE_DELIVERY`, stores the code, and returns whether this is the first shipping code.</criterion>
    <criterion>`OrderDomainHelper.test.ts` asserts card description includes customer name, mobile, address, item count/type, COD amount with locale formatting, and note.</criterion>
    <criterion>The automated test command exits 0.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Route existing useOrder calculations through pure helpers</name>
  <files>src/Hooks/useOrder.ts, src/Hooks/useOrder.test.ts</files>
  <read_first>src/Hooks/useOrder.ts, src/Hooks/useOrder.test.ts, src/Common/Helpers/OrderDomainHelper.ts, src/Common/Helpers/OrderDomainHelper.test.ts, src/Store/Reducers/OrderReducer.ts, src/Store/Reducers/CustomerReducer.ts</read_first>
  <action>Update `src/Hooks/useOrder.ts` to delegate pure logic to `OrderDomainHelper` while preserving current method names and return conventions for this plan. Replace `_buildDesc`, `_getLabelIds` internals, `calculateOrderPaymentAmount`, `getAutoCODAmount`, eligibility checks, `markOrderAsWaitingForReturn`, `markOrderAsReturned`, `markOrderAsPayCOD`, and `refund` local mutation logic with helper outputs followed by the same Redux dispatch calls. For async Trello workflows, use helper transition outputs before existing dispatch/Trello calls but keep the current `Promise<string>` or `Promise<TrelloCard>` signatures until Plan 03 introduces structured workflow results.</action>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runInBand src/Common/Helpers/OrderDomainHelper.test.ts src/Hooks/useOrder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/Hooks/useOrder.ts` imports `OrderDomainHelper` and no longer contains standalone implementations of card-description building, payment amount calculation, COD amount calculation, status eligibility checks, refund mutation, or COD-paid mutation.</criterion>
    <criterion>Existing public methods returned by `useOrder()` keep their current names so existing UI call sites compile.</criterion>
    <criterion>`refreshDoneOrders` tests in `src/Hooks/useOrder.test.ts` still pass unchanged or with only setup changes required by imports.</criterion>
    <criterion>The automated test command exits 0.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `CI=true yarn test --watchAll=false --runInBand src/Common/Helpers/OrderDomainHelper.test.ts src/Hooks/useOrder.test.ts` and confirm it exits 0. Then run `CI=true yarn test --watchAll=false --runInBand` to confirm broader Phase 1 tests still pass.
</verification>

<success_criteria>
- `ORD-01` is satisfied for the order rules extracted in this plan.
- `ORD-02` has pure tests for shipped, return, shipping code, COD paid, refund, amount, label, and card-description behavior.
- `useOrder` still exposes the existing API needed by current order screens.
- No Trello adapter, retry UI, backend, or broad UI redesign is introduced in this plan.
</success_criteria>

## Artifacts This Phase Produces

- `src/Common/Helpers/OrderDomainHelper.ts`
- `src/Common/Helpers/OrderDomainHelper.test.ts`
- `OrderDomainHelper.buildOrderTrelloDescription`
- `OrderDomainHelper.calculateOrderPaymentAmountFromCustomer`
- `OrderDomainHelper.getAutoCODAmount`
- `OrderDomainHelper.getOrderTrelloLabelKeys`
- `OrderDomainHelper.markOrderAsShippedTransition`
- `OrderDomainHelper.markOrderAsRefuseToReceiveTransition`
- `OrderDomainHelper.markOrderAsBrokenItemsTransition`
- `OrderDomainHelper.markOrderAsWaitingForReturnTransition`
- `OrderDomainHelper.markOrderAsReturnedTransition`
- `OrderDomainHelper.markOrderAsPayCODTransition`
- `OrderDomainHelper.changeShippingCodeTransition`
- `OrderDomainHelper.refundTransition`

