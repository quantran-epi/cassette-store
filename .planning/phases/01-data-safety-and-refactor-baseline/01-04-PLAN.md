---
phase: 01-data-safety-and-refactor-baseline
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/Hooks/useOrder.ts
  - src/Hooks/useOrder.test.ts
  - src/Routing/MasterPage.tsx
  - src/Routing/MasterPage.test.tsx
autonomous: false
requirements:
  - DATA-05
  - SYNC-04
user_setup: []
must_haves:
  truths:
    - "Operator sees loading, success, empty, and failure status for backup, restore, and done-order refresh."
    - "Operator sees last successful backup time and last restore result when available."
    - "Empty done-order refresh is displayed as a successful empty state, not an error."
    - "Failed done-order refresh preserves previously known done-order IDs."
    - "Status UI stays compact near existing drawer and floating backup/refresh controls."
  artifacts:
    - path: src/Hooks/useOrder.ts
      provides: "Atomic done-order refresh behavior"
    - path: src/Hooks/useOrder.test.ts
      provides: "Done-order refresh success, empty, and failure regression coverage"
    - path: src/Routing/MasterPage.tsx
      provides: "Compact persistent backup/restore/done-refresh status surface"
    - path: src/Routing/MasterPage.test.tsx
      provides: "Status rendering and restore/backup feedback coverage"
  key_links:
    - from: src/Hooks/useOrder.ts
      to: src/Store/Reducers/OrderReducer.ts
      via: "fetch Trello cards first, then dispatch the Plan 02 done-order replacement action only on success"
    - from: src/Routing/MasterPage.tsx
      to: src/Hooks/useOrder.ts
      via: "`_refreshDoneOrder` maps count/error results to transient messages and persistent status"
    - from: src/Routing/MasterPage.tsx
      to: localStorage.lastCheckTime
      via: "successful backup updates persistent last-backup timestamp shown to operator"
---

## Phase Goal

**As a** cassette-store operator and maintainer, **I want to** trust backup/restore and done-order refresh while test/build checks pass, **so that** refactoring can proceed without data loss.

<objective>
Add the small operator-facing status surface and finish done-order refresh safety.

Purpose: Plans 02 and 03 make backup/restore data-safe. This plan satisfies D-11 through D-14 and SYNC-04 by making success, empty, loading, and failure states visible without expanding into the Phase 4 operational status center.

Output: Atomic done-order refresh, compact persistent status near existing controls, and automated/manual verification for backup, restore, and done-refresh states.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/cassette-store/.codex/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/cassette-store/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-data-safety-and-refactor-baseline/01-CONTEXT.md
@.planning/phases/01-data-safety-and-refactor-baseline/01-RESEARCH.md
@.planning/phases/01-data-safety-and-refactor-baseline/01-VALIDATION.md
@src/Hooks/useOrder.ts
@src/Hooks/Trello/useTrello.ts
@src/Store/Reducers/OrderReducer.ts
@src/Routing/MasterPage.tsx
@src/Components/Button/Button.tsx
@src/Components/Layout/Stack/Stack.tsx
@src/Components/Typography
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make done-order refresh atomic and test-backed</name>
  <files>src/Hooks/useOrder.ts, src/Hooks/useOrder.test.ts</files>
  <read_first>src/Hooks/useOrder.ts, src/Hooks/Trello/useTrello.ts, src/Store/Reducers/OrderReducer.ts, src/Store/Store.ts</read_first>
  <behavior>
    - `refreshDoneOrders` calls `trello.getCardsByList` before mutating local `doneOrders`.
    - On Trello success with one or more `dueComplete` cards, local `doneOrders` is replaced with those card IDs and count is returned.
    - On Trello success with zero `dueComplete` cards, local `doneOrders` is replaced with `[]` and empty state is treated as success per D-12.
    - On Trello failure, local `doneOrders` remains unchanged and the error propagates or maps to a failure result for UI status per SYNC-04.
  </behavior>
  <action>Refactor `useOrder.refreshDoneOrders()` so it derives done IDs from the Trello response before dispatching. Use the Plan 02 done-order replacement action instead of `removeAllDoneOrder()` followed by per-ID `addDoneOrder()`; keep the exported hook API compatible with `MasterPage` or update `MasterPage` in the same plan if returning a structured result is clearer. Add tests that mock Trello success, empty success, and failure while asserting previous done-order IDs survive failure.</action>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runTestsByPath src/Hooks/useOrder.test.ts src/Store/Reducers/OrderReducer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test proves `removeAllDoneOrder()` is not dispatched before a failed Trello response can erase existing IDs.
    - Test proves empty Trello response returns or displays a non-error empty success state.
    - `useOrder.ts` still exports `refreshDoneOrders` through `src/Hooks/index.ts` without changing unrelated order workflows.
  </acceptance_criteria>
  <done>Done-order refresh cannot wipe local done-order IDs when Trello fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add compact persistent backup, restore, and refresh status</name>
  <files>src/Routing/MasterPage.tsx, src/Routing/MasterPage.test.tsx</files>
  <read_first>src/Routing/MasterPage.tsx, src/Components/Button/Button.tsx, src/Components/Layout/Stack/Stack.tsx, src/Components/Typography, src/Components/Tag, src/Hooks/useToggle.ts</read_first>
  <behavior>
    - Backup status shows loading, success, and failure near existing backup controls while preserving Ant Design transient messages per D-04 and D-11.
    - Restore status shows loading, success, failure, and last restore result near the restore drawer controls per D-11, D-12, and D-13.
    - Done-order refresh status shows loading, success with count, empty success, and failure near the floating refresh action per D-12.
    - User-facing copy follows existing concise Vietnamese app tone, while test names and technical identifiers may stay in English per D-14.
  </behavior>
  <action>Add local state in `MasterPage.tsx` for backup, restore, and done-refresh status, using existing Ant Design/local wrapper components (`Stack`, `Typography`, `Tag`, `Button`, `FloatButton`) instead of a new status center. Show last successful backup time from `localStorage.lastCheckTime` after successful backup and a last restore result timestamp/message after successful restore. Keep the surface compact and close to the drawer/floating controls; do not create the broader Phase 4 operational alerts area. Update tests to assert status text for backup success/failure, restore success/failure, done refresh count, empty, and failure.</action>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runTestsByPath src/Routing/MasterPage.test.tsx src/Hooks/useOrder.test.ts</automated>
    <human-check>Run the app, trigger backup/restore/done-refresh success, empty, and failure states with mocks or controlled inputs, and confirm the compact status surface is readable near existing drawer/floating controls on mobile and desktop widths.</human-check>
  </verify>
  <acceptance_criteria>
    - `MasterPage.test.tsx` covers loading or pending indicators plus success/empty/failure result text for backup, restore, and done refresh.
    - Existing transient `message.loading`, `message.success`, `message.info`, `message.warning`, and `message.error` behavior is preserved where applicable.
    - No new route, large dashboard panel, backend service, or backup provider is introduced.
  </acceptance_criteria>
  <done>Operators can see current and recent backup/restore/done-refresh outcomes without relying only on transient toast messages.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final Phase 1 local quality gates</name>
  <files>src/Hooks/useOrder.ts, src/Hooks/useOrder.test.ts, src/Routing/MasterPage.tsx, src/Routing/MasterPage.test.tsx</files>
  <read_first>package.json, .planning/phases/01-data-safety-and-refactor-baseline/01-VALIDATION.md, AGENTS.md</read_first>
  <action>After automated tests and human status placement verification pass, run the final local gates required by D-15 and the Phase 1 validation strategy. Do not deploy and do not copy generated build output into `docs/` unless the user explicitly asks to deploy.</action>
  <verify>
    <automated>CI=true yarn test --watchAll=false &amp;&amp; yarn build</automated>
  </verify>
  <acceptance_criteria>
    - Full Jest suite exits 0.
    - `yarn build` exits 0.
    - Summary records human verification result for compact status placement.
    - `git status --short` does not include generated `docs/` changes unless a separate deploy command was requested.
  </acceptance_criteria>
  <done>Phase 1 finishes with backup/restore/done-refresh visibility and green local gates.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- Atomic done-order refresh implementation in `useOrder.refreshDoneOrders`.
- Done-order replacement usage connected to the Plan 02 reducer action.
- Compact persistent backup status state and display in `MasterPage.tsx`.
- Compact persistent restore status state and display in `MasterPage.tsx`.
- Compact persistent done-refresh status state and display in `MasterPage.tsx`.
- `src/Hooks/useOrder.test.ts` coverage for done refresh success, empty, and failure preservation.
- `src/Routing/MasterPage.test.tsx` coverage for backup/restore/done-refresh status states.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trello TODO list to done-order state | Remote Trello cards determine local `doneOrders`. |
| Async backup/restore/refresh operations to operator status UI | Network outcomes become visible status messages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | `refreshDoneOrders` | mitigate | Replace local done-order IDs only after Trello cards are fetched and filtered successfully. |
| T-01-04-02 | Denial of Service | Status loading states | mitigate | Clear loading states in success and failure branches for backup, restore, and refresh. |
| T-01-04-03 | Repudiation | Operator feedback | mitigate | Persist last backup time and last restore result where feasible so transient messages are not the only evidence. |
</threat_model>

<verification>
Run these after all tasks in this plan:

1. `CI=true yarn test --watchAll=false --runTestsByPath src/Hooks/useOrder.test.ts src/Routing/MasterPage.test.tsx`
2. `CI=true yarn test --watchAll=false`
3. `yarn build`
4. Human verification checkpoint for compact status placement and copy.
</verification>

<success_criteria>
- DATA-05 is satisfied when backup/restore status, last backup time, and restore success/failure feedback are visible and tested.
- SYNC-04 is satisfied when done-order refresh has visible loading, success, empty, and failure states, and failure preserves prior IDs.
- D-04, D-05, D-11, D-12, D-13, D-14, and D-17 are covered in implementation, tests, and the human verification checkpoint.
</success_criteria>

<output>
Create `.planning/phases/01-data-safety-and-refactor-baseline/01-04-SUMMARY.md` when done.
</output>
