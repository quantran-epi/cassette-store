---
phase: 01-data-safety-and-refactor-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - craco.config.js
  - src/App.test.tsx
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - "Developer can run CI=true yarn test --watchAll=false without module-alias errors."
    - "The app smoke test verifies cassette-store shell behavior instead of the stale CRA sample text."
    - "Developer can run yarn build and confirm the production build still targets /cassette-store/."
  artifacts:
    - path: package.json
      provides: "Conservative Jest alias resolution for CRA one-shot tests"
    - path: craco.config.js
      provides: "Existing CRACO build/start aliases remain intact"
    - path: src/App.test.tsx
      provides: "App-specific smoke test for the provider/router shell"
  key_links:
    - from: package.json
      to: src/App.tsx
      via: "Jest moduleNameMapper resolves @store, @routing, @components, @modules, @common, and @hooks aliases"
    - from: src/App.test.tsx
      to: src/App.tsx
      via: "React Testing Library renders the app or a router-safe shell under required providers"
---

## Phase Goal

**As a** cassette-store operator and maintainer, **I want to** trust backup/restore and done-order refresh while test/build checks pass, **so that** refactoring can proceed without data loss.

<objective>
Repair the project test/build baseline before data-safety refactors begin.

Purpose: Phase 1 depends on fast feedback. This plan makes the one-shot Jest command usable per D-15, replaces the stale CRA sample assertion per D-16, and keeps tooling conservative per D-18.

Output: Passing app smoke test, Jest alias mapping for current TypeScript/CRACO aliases, and a documented build verification gate for static /cassette-store deployment.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/cassette-store/.codex/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/cassette-store/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-data-safety-and-refactor-baseline/01-CONTEXT.md
@.planning/phases/01-data-safety-and-refactor-baseline/01-RESEARCH.md
@.planning/phases/01-data-safety-and-refactor-baseline/01-VALIDATION.md
@.planning/codebase/TESTING.md
@package.json
@craco.config.js
@tsconfig.json
@src/App.tsx
@src/App.test.tsx
@src/Routing/RootRouter.tsx
@src/Routing/MasterPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair Jest alias resolution without changing the build stack</name>
  <files>package.json, craco.config.js</files>
  <read_first>package.json, craco.config.js, tsconfig.json, src/App.tsx, .planning/codebase/TESTING.md</read_first>
  <action>Add conservative Jest alias support for the existing aliases `@components`, `@routing`, `@modules`, `@store`, `@common`, and `@hooks` so `CI=true yarn test --watchAll=false` resolves imports from `src/App.tsx`. Prefer package-level CRA-supported `jest.moduleNameMapper` or CRACO test configuration already supported locally; do not add packages, do not migrate CRA/Jest to Vite/Vitest, and preserve the existing `start` and `build` scripts per D-15 and D-18. Keep `@hooks` mapped to `src/Hooks/index` to match the CRACO alias.</action>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runTestsByPath src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true yarn test --watchAll=false --runTestsByPath src/App.test.tsx` no longer fails with `Cannot find module '@store/Store'`.
    - `package.json` or `craco.config.js` contains mappings for all six aliases used by the app.
    - No new dependency is added to `package.json` or `yarn.lock` for alias resolution.
  </acceptance_criteria>
  <done>Jest can import the app provider tree and aliases in a one-shot CI-style test command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace stale CRA sample test with cassette-store smoke coverage</name>
  <files>src/App.test.tsx</files>
  <read_first>src/App.test.tsx, src/App.tsx, src/Routing/RootRouter.tsx, src/Routing/MasterPage.tsx, src/setupTests.ts</read_first>
  <behavior>
    - Test: rendering the app or a lightweight provider/router-safe shell exposes cassette-store operational chrome such as `Cua hang Cassette`, `Home`, `Don hang`, or `Khach hang` after normalizing existing Vietnamese text expectations.
    - Test: the stale `learn react` assertion is absent from `src/App.test.tsx`.
    - Test: the smoke path accounts for `BrowserRouter basename="/cassette-store"` or avoids basename coupling with a router-safe shell.
  </behavior>
  <action>Update `src/App.test.tsx` so it no longer checks for `learn react`. Render the real `<App />` if practical after alias repair; if Redux Persist or `BrowserRouter` makes the real app flaky under Jest/jsdom, create a lightweight smoke test that renders the same stable shell/provider route surface without network calls. Keep the assertion app-specific and provider-aware per D-16.</action>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runTestsByPath src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.test.tsx` does not contain `learn react`.
    - The test asserts visible cassette-store shell/navigation text or an equivalent stable app-shell marker.
    - The test passes in CI mode without watch prompts.
  </acceptance_criteria>
  <done>The single existing test is app-specific and proves the test runner can render a meaningful app shell.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the production build remains green</name>
  <files>package.json, craco.config.js, src/App.test.tsx</files>
  <read_first>package.json, craco.config.js, public/index.html, src/Routing/RootRouter.tsx, AGENTS.md</read_first>
  <action>Run the local quality gates required by Phase 1 after the alias and smoke-test changes. Keep deployment separate: do not copy `build/` into `docs/` and do not run deployment steps unless the user explicitly asks to deploy. Confirm the build output summary still reflects `/cassette-store/` static hosting per SAFE-02 and AGENTS.md.</action>
  <verify>
    <automated>CI=true yarn test --watchAll=false &amp;&amp; yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true yarn test --watchAll=false` exits 0.
    - `yarn build` exits 0.
    - Any build warnings are recorded in the plan summary, and no `docs/` generated output is modified during this non-deploy plan.
  </acceptance_criteria>
  <done>Developer has a passing one-shot test command and production build gate before broader refactors continue.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- Jest alias mapping for `@components`, `@routing`, `@modules`, `@store`, `@common`, and `@hooks` in the existing CRA-compatible test configuration.
- App-specific smoke test in `src/App.test.tsx`.
- Verification evidence in `.planning/phases/01-data-safety-and-refactor-baseline/01-01-SUMMARY.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local developer shell to test/build tools | Commands run local dependencies and generate local build output. |
| Jest/jsdom to app shell | Test runtime imports app code but must not invoke real Trello network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | Jest alias configuration | mitigate | Map only existing source aliases from `tsconfig.json`/`craco.config.js`; do not add dependency or broad catch-all mappings. |
| T-01-01-02 | Denial of Service | Jest command | mitigate | Use `CI=true` and `--watchAll=false` so verification is one-shot and non-interactive. |
| T-01-01-03 | Information Disclosure | Build output | accept | `yarn build` uses existing CRA behavior; deployment bundle changes are not committed in this non-deploy plan. |
</threat_model>

<verification>
Run these after all tasks in this plan:

1. `CI=true yarn test --watchAll=false`
2. `yarn build`
3. `git status --short` and confirm this plan did not modify `docs/` build output.
</verification>

<success_criteria>
- SAFE-01 is satisfied when `CI=true yarn test --watchAll=false` exits 0 without module-alias errors.
- SAFE-02 is satisfied when `yarn build` exits 0 and non-deploy generated output is not committed.
- SAFE-03 is satisfied when `src/App.test.tsx` is app-specific and no longer asserts `learn react`.
- D-15, D-16, and D-18 are cited in the implementation summary with command output.
</success_criteria>

<output>
Create `.planning/phases/01-data-safety-and-refactor-baseline/01-01-SUMMARY.md` when done.
</output>
