---
phase: 03-fast-order-and-shipping-workflows
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/Modules/Order/Screens/OrderItem/OrderItem.widget.tsx
  - src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.tsx
  - src/Modules/Order/Screens/OrderItem/OrderChangeShippingCode.widget.tsx
  - src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.test.tsx
  - src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.test.tsx
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - OPS-02: operator can update a shipping code from the order row and immediately see local/Trello status.
    - D-13-D-16: inline row shipping entry is primary, paste is explicit, and first-code Trello behavior stays on `useOrder.changeShippingCode`.
    - D-19-D-20: sync warnings remain row-scoped and expose both Thử lại and Đã xử lý through Phase 2 recovery controls.
  artifacts:
    - src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.tsx provides row-level save and explicit paste controls.
    - src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.test.tsx covers row entry, explicit clipboard paste, and local-success/Trello-failure status.
    - src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.test.tsx preserves retry and manual-resolved recovery controls.
  key_links:
    - OrderItem.widget.tsx -> OrderInlineShippingCodeWidget -> existing change-shipping-code handler -> useOrder.changeShippingCode.
    - OrderChangeShippingCode.widget.tsx paste button -> shared explicit clipboard-read behavior -> no automatic read on open.
    - useOrder.changeShippingCode result -> OrderSyncStatusWidget row-scoped recovery controls.
---

# Phase 03 Plan 03: Inline Shipping-Code Flow and Row Status

## Phase Goal

**As a** cassette store operator, **I want to** enter shipping codes directly on an order row, **so that** I can save codes quickly and see whether local state and Trello sync succeeded.

<objective>
Add a row-level shipping-code quick entry that uses the existing `useOrder.changeShippingCode` local-first workflow, replaces automatic clipboard reads with explicit paste, and keeps Trello failure feedback compact on the affected order row.
</objective>

<context>
Read Phase 3 CONTEXT decisions D-13 through D-20 and Phase 2 sync recovery summary. Current source facts: `OrderItemWidget` already calls `orderUtils.changeShippingCode`, displays `OrderSyncStatusWidget`, and shows saved shipping code; `OrderChangeShippingCodeWidget` currently auto-reads clipboard on open; `OrderSyncStatusWidget` already contains `Thử lại` and `Đã xử lý` actions.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Cover row-level shipping-code save behavior</name>
  <files>src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.test.tsx, src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.test.tsx</files>
  <read_first>
    - src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.test.tsx
    - src/Modules/Order/Screens/OrderItem/OrderItem.widget.tsx
    - src/Modules/Order/Screens/OrderItem/OrderChangeShippingCode.widget.tsx
    - src/Hooks/OrderWorkflowResult.ts
    - src/Store/Models/OrderSyncFailure.ts
  </read_first>
  <action>Create `OrderInlineShippingCode.widget.test.tsx` using the existing `OrderSyncStatus.widget.test.tsx` mock/store pattern. Cover D-13 by rendering a placed, Trello-pushed order with an obvious row-level shipping-code input/action. Cover D-14 by mocking `navigator.clipboard.readText` and asserting it is called only after the explicit paste button is clicked, not when the widget opens. Cover D-16 by mocking `changeShippingCode` to return `localUpdated: true` with a `create-comment` or `move-card` sync failure and asserting warning feedback plus row-scoped sync failure text remains visible. Extend `OrderSyncStatus.widget.test.tsx` only if needed to assert both `Thử lại` and `Đã xử lý` remain present for shipping failures per D-19.</action>
  <acceptance_criteria>
    - New tests assert no automatic clipboard read on open/render.
    - New tests assert explicit paste reads clipboard and populates the shipping code field.
    - New tests assert local-success/Trello-failure calls warning feedback and displays row-scoped sync controls.
  </acceptance_criteria>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runTestsByPath src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.test.tsx src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.test.tsx</automated>
  </verify>
  <done>Shipping widget tests prove row entry is visible, clipboard reads happen only via explicit paste, and local-success/Trello-failure shows row-scoped recovery controls.</done>
</task>

<task type="auto">
  <name>Task 2: Add primary inline shipping-code widget</name>
  <files>src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.tsx, src/Modules/Order/Screens/OrderItem/OrderItem.widget.tsx</files>
  <read_first>
    - src/Modules/Order/Screens/OrderItem/OrderItem.widget.tsx
    - src/Modules/Order/Screens/OrderItem/OrderChangeShippingCode.widget.tsx
    - src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.tsx
    - src/Hooks/useOrder.ts
    - src/Hooks/OrderWorkflowResult.ts
    - src/Common/Constants/AppConstants.ts
  </read_first>
  <action>Create `OrderInlineShippingCodeWidget` for D-13 with props for `order`, `loading`, `value`, `disabled`, `onSave`, and optional `onOpenModal`. Render it in `OrderItemWidget` near the existing shipping-code display/status metadata for orders that are Trello-pushed and do not yet have a shipping code, and keep the existing dropdown/modal as a secondary focused path. Use a compact `Input` plus icon buttons for save and explicit paste. Save must call the same `_onChangeShippingCode` handler currently used by `OrderChangeShippingCodeWidget`, so D-15 remains on `useOrder.changeShippingCode` and `OrderDomainHelper.changeShippingCodeTransition`. When `result.localUpdated` is true, clear the inline field/loading state but do not hide row details or remove the saved code display. Reuse `_showWorkflowResult` so local failure is an error and local-success/Trello-failure is a warning.</action>
  <acceptance_criteria>
    - `OrderItemWidget` renders an inline shipping-code entry for Trello-pushed placed orders without a shipping code.
    - Inline save calls `orderUtils.changeShippingCode(props.item.id, code)` through the existing `_onChangeShippingCode` path.
    - Existing modal path remains reachable from the dropdown for focused editing.
    - Saved shipping code remains visible in the row after local success.
  </acceptance_criteria>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runTestsByPath src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.test.tsx</automated>
  </verify>
  <done>`OrderInlineShippingCodeWidget` is rendered from `OrderItemWidget`, calls the existing change-shipping-code handler, keeps modal editing available, and leaves saved codes visible.</done>
</task>

<task type="auto">
  <name>Task 3: Replace automatic clipboard read with explicit paste and verify row-scoped feedback</name>
  <files>src/Modules/Order/Screens/OrderItem/OrderChangeShippingCode.widget.tsx, src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.tsx, src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.test.tsx</files>
  <read_first>
    - src/Modules/Order/Screens/OrderItem/OrderChangeShippingCode.widget.tsx
    - src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.tsx
    - src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.tsx
    - src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.test.tsx
  </read_first>
  <action>Remove the `afterOpenChange` automatic `navigator.clipboard.readText()` behavior from `OrderChangeShippingCodeWidget` for D-14. Add an explicit paste button to the modal using the same paste handler shape as `OrderInlineShippingCodeWidget`. Ensure both modal and inline flows keep D-18 quiet success behavior: use existing transient success/warning/error messages and do not add persistent success badges. Verify D-19 and D-20 by keeping all persistent Trello failure display in `OrderSyncStatusWidget` on the affected order row and not creating a list-wide alert/status center.</action>
  <acceptance_criteria>
    - `OrderChangeShippingCodeWidget` no longer calls `navigator.clipboard.readText()` from `afterOpenChange`.
    - Both inline and modal shipping-code surfaces expose an explicit paste control.
    - `OrderSyncStatusWidget` continues to show `Thử lại` and `Đã xử lý` for shipping-related failures.
    - No new order-list-wide sync alert/status center is introduced.
  </acceptance_criteria>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runTestsByPath src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.test.tsx src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.test.tsx src/Hooks/useOrder.test.ts</automated>
  </verify>
  <done>Both shipping-code surfaces use explicit paste, automatic clipboard reads are removed, and persistent status remains limited to the affected order row.</done>
</task>
</tasks>

<verification>
- Run `CI=true yarn test --watchAll=false --runTestsByPath src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.test.tsx src/Modules/Order/Screens/OrderItem/OrderSyncStatus.widget.test.tsx`.
- Run `CI=true yarn test --watchAll=false --runTestsByPath src/Hooks/useOrder.test.ts` to prove shipping-code local/Trello behavior remains on the existing domain hook path.
</verification>

<success_criteria>
- Operator can enter a shipping code directly from an order row.
- Clipboard access happens only from an explicit paste action.
- First shipping-code save preserves local update, Trello comment, and Trello card move behavior through `useOrder.changeShippingCode`.
- Local-success/Trello-failure leaves the saved shipping code visible and shows compact row-scoped retry/manual-resolved controls.
</success_criteria>

## Artifacts this phase produces

- `src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.tsx`
- `src/Modules/Order/Screens/OrderItem/OrderInlineShippingCode.widget.test.tsx`
- Explicit paste handler(s) for inline and modal shipping-code entry
- Row-level shipping-code rendering branch in `OrderItem.widget.tsx`

## Source Audit

| Source | ID | Coverage | Plan |
|--------|----|----------|------|
| GOAL | Faster shipping updates with clear local/Trello status | Covered by inline save and row-scoped sync warning | 03-03 |
| REQ | OPS-02 | Covered by direct row shipping-code entry and status tests | 03-03 |
| CONTEXT | D-13-D-16 | Covered by inline primary path, explicit paste, and existing `changeShippingCode` domain path | 03-03 |
| CONTEXT | D-18-D-20 | Covered by quiet success, existing row-scoped warning controls, and no broad alert center | 03-03 |
| RESEARCH | Reuse Phase 2 recovery | Covered by `OrderSyncStatusWidget` and `OrderWorkflowResult` handling | 03-03 |
