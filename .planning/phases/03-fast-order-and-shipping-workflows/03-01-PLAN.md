---
phase: 03-fast-order-and-shipping-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/Order/Routing/OrderRouteConfig.ts
  - src/Routing/RootRouter.tsx
  - src/Routing/RootRouter.test.tsx
autonomous: true
requirements: [ORD-05]
must_haves:
  truths:
    - ORD-05: order route config exports order-specific route names while preserving existing path strings.
    - D-06: RootRouter mounts order routes through OrderRouter, not CustomerRouter, without breaking /order/list, /order/create, or /order/cod-payment-list.
    - Route smoke coverage proves the order branch renders from the GitHub Pages basename /cassette-store.
  artifacts:
    - src/Modules/Order/Routing/OrderRouteConfig.ts exposes the `OrderRoutes` local/default export.
    - src/Routing/RootRouter.tsx imports and mounts `OrderRouter` for the order route root.
    - src/Routing/RootRouter.test.tsx covers all three `/order/*` routes with distinct order/customer router markers.
  key_links:
    - OrderRouteConfig.ts -> RootRoutes.AuthorizedRoutes.OrderRoutes -> RootRouter.tsx order branch -> OrderRouter -> order screens.
    - RootRouter.test.tsx static route-config assertion -> OrderRouteConfig.ts symbol/export naming.
---

# Phase 03 Plan 01: Correct Order Routes and Smoke Coverage

## Phase Goal

**As a** cassette store operator, **I want to** navigate order routes through order-specific routing, **so that** route cleanup does not break fast order and shipping workflows.

<objective>
Correct the low-risk route naming/layout mismatch before workflow changes: `OrderRouteConfig.ts` must expose order-specific route symbols, and `RootRouter.tsx` must render the order branch with `OrderRouter` while keeping all existing order paths stable.
</objective>

<context>
Read Phase 3 CONTEXT decisions D-06 and requirement ORD-05 first. Current source facts: `src/Modules/Order/Routing/OrderRouteConfig.ts` defines `/order` paths through a `CustomerRoutes` constant, and `src/Routing/RootRouter.tsx` mounts the order route root with `CustomerRouter` even though `OrderRouter` exists.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add route smoke coverage for stable order paths</name>
  <files>src/Routing/RootRouter.test.tsx</files>
  <read_first>
    - src/App.test.tsx
    - src/Routing/RootRouter.tsx
    - src/Routing/RootRoutes.ts
    - src/Modules/Order/Routing/OrderRouteConfig.ts
    - src/Modules/Order/Routing/OrderRouter.tsx
    - src/Modules/Customer/Routing/CustomerRouter.tsx
  </read_first>
  <action>Create `src/Routing/RootRouter.test.tsx` using React Testing Library patterns from `src/App.test.tsx`. Mock `idb-keyval`, `nanoid`, and browser fetch the same way as `App.test.tsx`. Add distinct Jest module mocks for `@modules/Order/Routing/OrderRouter` and `@modules/Customer/Routing/CustomerRouter`; the order mock must render `data-testid="order-router"`, and the customer mock must render `data-testid="customer-router"`, both with an `<Outlet />` so child routes still render. Seed `window.history.pushState` with `/cassette-store/order/list`, `/cassette-store/order/create`, and `/cassette-store/order/cod-payment-list` in separate tests. Assert each order path renders `order-router` and does not render `customer-router`, making the current D-06 `CustomerRouter` order-branch bug observable. Add a focused static assertion using `fs.readFileSync` for `src/Modules/Order/Routing/OrderRouteConfig.ts` that fails while the file contains `const CustomerRoutes` or `export default CustomerRoutes`, then passes when the local symbol/export is `OrderRoutes`.</action>
  <acceptance_criteria>
    - `src/Routing/RootRouter.test.tsx` exists and imports `RootRouter` or `App` through the same provider-safe pattern used by existing tests.
    - The test file mocks `OrderRouter` and `CustomerRouter` with distinct markers and asserts order paths render the order marker, not the customer marker.
    - The test file contains a static source assertion that `OrderRouteConfig.ts` no longer contains `const CustomerRoutes` or `export default CustomerRoutes`.
    - The file contains tests for `/cassette-store/order/list`, `/cassette-store/order/create`, and `/cassette-store/order/cod-payment-list`.
    - The route tests still verify child route behavior enough to prove paths remain stable after the mocked router marker renders.
  </acceptance_criteria>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runTestsByPath src/Routing/RootRouter.test.tsx</automated>
  </verify>
  <done>Route tests fail against the current `CustomerRouter` order branch and `CustomerRoutes` route-config symbol, then pass only when D-06 route component and name corrections are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Rename order route config symbols without changing paths</name>
  <files>src/Modules/Order/Routing/OrderRouteConfig.ts, src/Routing/RootRoutes.ts</files>
  <read_first>
    - src/Modules/Order/Routing/OrderRouteConfig.ts
    - src/Modules/Customer/Routing/CustomerRouteConfig.ts
    - src/Routing/RootRoutes.ts
    - src/Common/Helpers/RouteHelper.ts
  </read_first>
  <action>In `src/Modules/Order/Routing/OrderRouteConfig.ts`, rename the local constant from `CustomerRoutes` to `OrderRoutes` and export `OrderRoutes` as the default. Preserve the root string `/order` and route segments `list`, `create`, and `cod-payment-list` exactly for D-06 and ORD-05. In `src/Routing/RootRoutes.ts`, keep the public `AuthorizedRoutes.OrderRoutes` key and import name stable so existing callers continue using `RootRoutes.AuthorizedRoutes.OrderRoutes.*()`.</action>
  <acceptance_criteria>
    - `src/Modules/Order/Routing/OrderRouteConfig.ts` contains `const OrderRoutes = RouteHelpers.CreateRoutes('/order'`.
    - `src/Modules/Order/Routing/OrderRouteConfig.ts` contains `export default OrderRoutes` and no `CustomerRoutes` identifier.
    - `RootRoutes.AuthorizedRoutes.OrderRoutes.List()` still resolves to `/order/list`; `Create()` still resolves to `/order/create`; `CodPaymentList()` still resolves to `/order/cod-payment-list`.
  </acceptance_criteria>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runTestsByPath src/Routing/RootRouter.test.tsx</automated>
  </verify>
  <done>Order route config exports `OrderRoutes`, contains no `CustomerRoutes` identifier, and preserves all existing `/order/*` path strings.</done>
</task>

<task type="auto">
  <name>Task 3: Mount order routes through OrderRouter</name>
  <files>src/Routing/RootRouter.tsx, src/Routing/RootRouter.test.tsx</files>
  <read_first>
    - src/Routing/RootRouter.tsx
    - src/Modules/Order/Routing/OrderRouter.tsx
    - src/Modules/Customer/Routing/CustomerRouter.tsx
    - src/Routing/RootRouter.test.tsx
  </read_first>
  <action>Update `src/Routing/RootRouter.tsx` so the order route branch imports and renders `OrderRouter` for `RootRoutes.AuthorizedRoutes.OrderRoutes.Root()`. Keep the customer branch on `CustomerRouter`. Preserve child route elements for `OrderListScreen`, `OrderCreateScreen`, and `OrderCodPaymentListScreen`. This completes D-06 by correcting route layout components without path changes.</action>
  <acceptance_criteria>
    - `RootRouter.tsx` imports `OrderRouter` from `@modules/Order/Routing/OrderRouter`.
    - The route whose path is `RootRoutes.AuthorizedRoutes.OrderRoutes.Root()` has `element={<OrderRouter />}`.
    - The route whose path is `RootRoutes.AuthorizedRoutes.CustomerRoutes.Root()` still has `element={<CustomerRouter />}`.
    - Route tests for all three order paths pass and assert `order-router`, not `customer-router`, for order routes.
  </acceptance_criteria>
  <verify>
    <automated>CI=true yarn test --watchAll=false --runTestsByPath src/Routing/RootRouter.test.tsx src/App.test.tsx</automated>
  </verify>
  <done>`RootRouter.tsx` mounts the order branch with `OrderRouter`, leaves the customer branch on `CustomerRouter`, and route smoke tests pass.</done>
</task>
</tasks>

<verification>
- Run `CI=true yarn test --watchAll=false --runTestsByPath src/Routing/RootRouter.test.tsx src/App.test.tsx`.
- Run `yarn build` after Plan 03-01 or at the end of the wave to catch stale imports from the route rename.
</verification>

<success_criteria>
- `/cassette-store/order/list`, `/cassette-store/order/create`, and `/cassette-store/order/cod-payment-list` still resolve through the app shell and the mocked order router marker.
- Order route config uses order-specific naming while public route methods remain stable.
- The order route branch uses `OrderRouter`, and the customer branch still uses `CustomerRouter`.
</success_criteria>

## Artifacts this phase produces

- `src/Routing/RootRouter.test.tsx`
- `OrderRoutes` local route config symbol in `src/Modules/Order/Routing/OrderRouteConfig.ts`
- `OrderRouter` import/use in `src/Routing/RootRouter.tsx`

## Source Audit

| Source | ID | Coverage | Plan |
|--------|----|----------|------|
| GOAL | Route stability for faster workflows | Covered by route smoke tests and `OrderRouter` mounting | 03-01 |
| REQ | ORD-05 | Covered by order-specific route names/components | 03-01 |
| CONTEXT | D-06 | Covered by symbol rename, `OrderRouter` branch, and path-preserving assertions | 03-01 |
| RESEARCH | Route cleanup low risk | Covered by preserving exact `/order/*` path strings | 03-01 |
